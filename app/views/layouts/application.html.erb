<!DOCTYPE html>
<html>
  <head>
    <title>Stonky</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>

    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div class="flex flex-col items-center <%= signed_in? ? 'justify-start mt-8' : 'justify-center' %> h-screen px-4">
      <% if signed_in? %>
        <div class="flex items-center w-full max-w-md py-2 mb-8 border-b border-gray-200">
          <h2 class="text-2xl font-bold">Hey, <%= current_user.username %></h2>
          <%= link_to "Sign out", session_path,
                                  method: :delete,
                                  class: [
                                    "bg-transparent",
                                    "border",
                                    "border-green-500",
                                    "duration-300",
                                    "ease-in-out",
                                    "focus:bg-green-500",
                                    "focus:text-white",
                                    "hover:bg-green-500",
                                    "hover:text-white",
                                    "ml-auto",
                                    "px-4",
                                    "py-2",
                                    "rounded",
                                    "text-green-500",
                                    "text-white",
                                    "transition-colors"
                                  ].join(" ") %>
        </div>
      <% end %>

      <%= render partial: "shared/flash_messages" %>

      <%= yield %>
    </div>
  </body>
</html>
